function skillsMember() {
  // Get the member's skills
  let skills = this.skills;
  // Get the member's name
  let name = this.name;
  // Return the member's name and skills
  console.log(`${name}'s skills: ${skills}`);
}
